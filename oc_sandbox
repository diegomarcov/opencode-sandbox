#!/usr/bin/env bash
set -euo pipefail
img="${OC_IMAGE:-opencode-sandbox:local}"

exec docker run --rm -it \
  -v "$PWD:/work" \
  -v opencode-state:/state \
  -e HOME=/state/home \
  -w /work \
  "$img" \
  bash -lc 'mkdir -p "$HOME" && opencode'
